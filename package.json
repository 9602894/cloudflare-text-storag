{
  "name": "cloudflare-text-storage",
  "version": "1.0.0",
  "description": "完整文本存储系统 - 支持文件转链接、搜索管理和备注功能",
  "main": "functions/[[path]].js",
  "scripts": {
    "dev": "wrangler pages dev public --binding FILES=files",
    "build": "echo 'No build step required for Pages'",
    "deploy": "echo 'Deployment is handled by Cloudflare Pages'"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
